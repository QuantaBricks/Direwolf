# Not needed to build/run Engine inside the image; keeping them out of the
# build context is what actually matters for docker build speed (final
# image size is controlled by the multi-stage COPY list in Dockerfile,
# not by this file).
.git
.claude
referenceDir
release
benchmark
test
docs
report
build
*.mod
*.o
fort.*
